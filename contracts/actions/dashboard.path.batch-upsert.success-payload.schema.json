{
  "$schema": "https://json-schema.org/draft/2020-12/schema", "$id": "https://dashboard.local/schemas/actions/dashboard.path.batch-upsert/2.0/success-payload.json", "title": "dashboard.path.batch-upsert success payload", "type": "object", "additionalProperties": false, "required": ["aggregateRevision", "items"], "properties": { "aggregateRevision": { "type": "integer", "minimum": 1 }, "items": { "type": "array", "minItems": 1, "maxItems": 50, "items": { "type": "object", "additionalProperties": false, "required": ["target", "id"], "properties": { "target": { "enum": ["path", "project"] }, "id": { "type": "string", "pattern": "^[a-z0-9][a-z0-9-]{0,95}$" } } } } }
}
