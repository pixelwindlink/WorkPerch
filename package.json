{
  "name": "perch",
  "version": "2.2.0",
  "private": true,
  "type": "module",
  "main": "electron/main.mjs",
  "description": "通过 EngineMessage 管理本地开发路径、速记与项目入口目录的 WorkPerch。",
  "scripts": {
    "start": "node server.mjs",
    "dev:server": "node server.mjs",
    "dev": "node --watch server.mjs",
    "desktop": "electron .",
    "desktop:pack": "node scripts/package-desktop.mjs",
    "desktop:release": "node scripts/build-release.mjs",
    "check": "node --check server.mjs && node --check cli.mjs && node --check app.js && for f in ui/*.js; do node --check \"$f\" || exit 1; done && node --check electron/main.mjs && node --check electron/finder-path-controller.mjs && node --check electron/local-engine-client.mjs && node --check electron/server-coordinator.mjs && node --check electron/window-state.mjs && node --check electron/preload.cjs && node --check scripts/package-desktop.mjs && node --check scripts/build-release.mjs && node --check src/composition/create-perch-engine.mjs && node --check src/inbound/dispatcher.mjs && node --check src/application/perch-application.mjs && node --check src/domain/perch-aggregate.mjs",
    "test": "node --test tests/**/*.test.mjs",
    "test:unit": "node --test tests/unit/*.test.mjs",
    "test:contract": "node --test tests/contract/*.test.mjs",
    "test:integration": "node --test tests/integration/*.test.mjs",
    "test:e2e": "node --test tests/e2e/*.test.mjs"
  },
  "engines": {
    "node": ">=22.13.0"
  },
  "devDependencies": {
    "@electron/packager": "^20.0.4",
    "electron": "^43.2.0"
  }
}
