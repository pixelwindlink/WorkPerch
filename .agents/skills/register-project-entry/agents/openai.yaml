interface:
  display_name: "Register Perch Project"
  short_description: "Register projects through Perch EngineMessage"
  default_prompt: "Use $register-project-entry to register the current project in WorkPerch and verify the saved entry."
