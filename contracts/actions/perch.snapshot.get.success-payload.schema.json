{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://perch.local/schemas/actions/perch.snapshot.get/1.0/success-payload.json",
  "title": "perch.snapshot.get success payload",
  "type": "object",
  "additionalProperties": false,
  "required": ["schemaVersion", "aggregateRevision", "createdAt", "updatedAt"],
  "properties": {
    "schemaVersion": { "const": "1.0" },
    "aggregateRevision": { "type": "integer", "minimum": 0 },
    "createdAt": { "type": "string", "minLength": 20, "maxLength": 40 },
    "updatedAt": { "type": "string", "minLength": 20, "maxLength": 40 },
    "groups": {
      "type": "array",
      "maxItems": 500,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["id", "name", "color", "createdAt", "updatedAt"],
        "properties": {
          "id": { "type": "string", "pattern": "^[a-z0-9][a-z0-9-]{0,95}$" },
          "name": { "type": "string", "minLength": 1, "maxLength": 80 },
          "color": { "type": "string", "pattern": "^#[0-9A-Fa-f]{6}$" },
          "createdAt": { "type": "string", "minLength": 20, "maxLength": 40 },
          "updatedAt": { "type": "string", "minLength": 20, "maxLength": 40 }
        }
      }
    },
    "paths": {
      "type": "array",
      "maxItems": 5000,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["id", "name", "path", "groupId", "group", "description", "pinned", "createdAt", "updatedAt"],
        "properties": {
          "id": { "type": "string", "pattern": "^[a-z0-9][a-z0-9-]{0,95}$" },
          "name": { "type": "string", "minLength": 1, "maxLength": 120 },
          "path": { "type": "string", "minLength": 1, "maxLength": 4096, "pattern": "^/" },
          "groupId": { "type": "string", "pattern": "^[a-z0-9][a-z0-9-]{0,95}$" },
          "group": { "type": "string", "minLength": 1, "maxLength": 80 },
          "description": { "type": "string", "maxLength": 500 },
          "pinned": { "type": "boolean" },
          "createdAt": { "type": "string", "minLength": 20, "maxLength": 40 },
          "updatedAt": { "type": "string", "minLength": 20, "maxLength": 40 }
        }
      }
    },
    "notes": {
      "type": "array",
      "maxItems": 5000,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["id", "title", "content", "pinned", "createdAt", "updatedAt"],
        "properties": {
          "id": { "type": "string", "pattern": "^[a-z0-9][a-z0-9-]{0,95}$" },
          "title": { "type": "string", "minLength": 1, "maxLength": 120 },
          "content": { "type": "string", "minLength": 1, "maxLength": 10000 },
          "pinned": { "type": "boolean" },
          "createdAt": { "type": "string", "minLength": 20, "maxLength": 40 },
          "updatedAt": { "type": "string", "minLength": 20, "maxLength": 40 }
        }
      }
    },
    "projects": {
      "type": "array",
      "maxItems": 2000,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["id", "name", "type", "label", "description", "path", "url", "port", "command", "tags", "pinned", "createdAt", "updatedAt"],
        "properties": {
          "id": { "type": "string", "pattern": "^[a-z0-9][a-z0-9-]{0,95}$" },
          "name": { "type": "string", "minLength": 1, "maxLength": 120 },
          "type": { "enum": ["engine", "candidate", "tool", "application", "workspace", "archive", "other"] },
          "label": { "type": "string", "minLength": 1, "maxLength": 80 },
          "description": { "type": "string", "maxLength": 500 },
          "path": { "type": "string", "minLength": 1, "maxLength": 4096, "pattern": "^/" },
          "url": { "type": "string", "maxLength": 2048, "pattern": "^(?:https?://.*)?$" },
          "port": { "type": "integer", "minimum": 0, "maximum": 65535 },
          "command": { "type": "string", "maxLength": 2000 },
          "tags": { "type": "array", "maxItems": 30, "uniqueItems": true, "items": { "type": "string", "minLength": 1, "maxLength": 50 } },
          "pinned": { "type": "boolean" },
          "createdAt": { "type": "string", "minLength": 20, "maxLength": 40 },
          "updatedAt": { "type": "string", "minLength": 20, "maxLength": 40 }
        }
      }
    }
  }
}
